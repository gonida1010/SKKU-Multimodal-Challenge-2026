# 맥북 로컬 개발용 (가벼운 의존성만).
# 주의: 맥에는 CUDA가 없어 vLLM 기반 실제 모델 추론은 불가합니다.
# 로컬에서는 데이터 점검 / unknown 탐지기 / BBQ 다운로드·채점 로직 / 코드 편집만 가능.
# 실제 모델 추론(evaluate.py, run_inference.py + 모델 로드)은 Colab/A6000에서

pillow>=10.0.0      # 이미지 로딩 테스트용
tqdm>=4.66.0
# (BBQ 다운로드/CSV 파싱/JSON은 표준 라이브러리만 사용하므로 추가 설치 불필요)
