# 제출 대상 기준 환경
# OS: Ubuntu 20.04
# Python: 3.10
# System CUDA: 12.4
# GPU: NVIDIA RTX A6000 48GB
#
# Qwen3.5는 vLLM 0.8.x에서 지원되지 않는다.
# Qwen3.5를 지원하는 최소 안정 버전인 vLLM 0.17.0은
# PyTorch 2.10.0 / torchvision 0.25.0 / torchaudio 2.10.0을 요구한다.
# vLLM 0.17.0 공식 PyPI wheel과 일치시키고 CUDA 13 wheel 선택을 막기 위해
# CUDA 12 계열의 cu129 PyTorch wheel을 고정한다.

--extra-index-url https://download.pytorch.org/whl/cu129

torch==2.10.0+cu129
torchvision==0.25.0+cu129
torchaudio==2.10.0+cu129
vllm==0.17.0
transformers==4.57.0
pillow==10.4.0
tqdm==4.67.1
